# Utils Directory

Place stateless, pure helper utilities here (date formatting, string helpers, calculations, etc.).
Unlike `services/`, utils should not maintain state or execute asynchronous network operations.
