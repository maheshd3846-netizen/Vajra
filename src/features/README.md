# Features Directory

Place self-contained feature modules here. Each feature should encapsulate its own components, hooks, state, or simple routing structure if applicable.

Example:
```
src/features/
  auth/
    components/
    hooks/
    utils/
  dashboard/
    components/
```
