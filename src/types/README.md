# Types Directory

Place global TypeScript interfaces, type definitions, schemas, and API response templates here.
Database row definitions generated from Supabase can also be placed here or in `src/supabase/types.ts`.
